# RLVR Project Dependencies
# ===========================

# Core
torch>=2.0.0
transformers>=4.40.0
datasets>=2.0.0
numpy>=1.24.0

# Tinker Platform
tinker  # pip install tinker

# VERL Framework (optional, install from source for full features)
# git clone https://github.com/volcengine/verl && cd verl && pip install -e .

# Inference Acceleration (Optional)
# vllm>=0.4.0  # for local model evaluation with scripts/eval/evaluate_math.py --model
# sglang  # alternative inference engine

# Training Utilities (Optional - for local training with VERL, not needed for Tinker)
# accelerate>=0.20.0
# peft>=0.5.0  # for LoRA
# bitsandbytes>=0.40.0  # for quantization
# flash-attn>=2.0.0  # for flash attention

# Logging & Visualization
wandb>=0.15.0
tensorboard>=2.13.0
tqdm>=4.65.0

# Evaluation
lighteval  # for standard benchmarks
sympy  # for symbolic math verification

# Development
ipython>=8.0.0
jupyter>=1.0.0
pytest>=7.0.0
black>=23.0.0
isort>=5.12.0

# Documentation
mkdocs>=1.4.0
mkdocs-material>=9.0.0
